import React from 'react';
import { connect } from 'react-redux';

function EntriesList({ dispatch, entries }) {

};

const mapStateToProps = (state) => ({

})

export default connect()(EntriesList);
